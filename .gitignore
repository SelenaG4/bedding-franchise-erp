# The live SQLite file -- fully regenerated by scripts/seed.py (and
# scripts/generate_demand_history.py for the demand-history CSV) on every
# deploy. Committing this file is what let it drift out of sync with the
# schema in app/models.py in the past -- see the Render deploy fix.
bedding_erp.db
bedding_erp.db-*

# Python
__pycache__/
*.pyc
.pytest_cache/
